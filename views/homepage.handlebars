<!-- Loop over each object in the posts array -->
{{#each posts as |post| }}
<div>
  <div>
    <h2>{{post.title}}</h2>
    <p>
      {{post.content}}
    </p>
    <p>Created by {{post.user.username}} on {{post.date_created}}</p>
  </div>
</div>
{{/each}}
